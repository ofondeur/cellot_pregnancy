data:
  features: ./datasets/sherlock_training_data/features.txt
  path: ./datasets/sherlock_training_data/combined_IL_NK.h5ad
  condition: drug
  source: Unstim
  type: cell

dataloader:
  batch_size: 256
  shuffle: true

datasplit:
  groupby: drug
  name: train_test
  test_size: 0.2
